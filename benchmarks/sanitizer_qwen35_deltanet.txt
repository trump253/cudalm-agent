# compute-sanitizer memcheck — CUDALM v0.2 Phase C (Qwen3.5 Gated DeltaNet)
#
# Host: RTX 2080 Ti (sm_75), CUDA 11.8, single device (CUDA_VISIBLE_DEVICES=0)
# Tool: /usr/local/cuda-11.8/bin/compute-sanitizer --tool memcheck
#        --error-exitcode 99
#
# Scope: every new Phase C DeltaNet kernel + the full Qwen35DeltaNetLayer
# memory surface (conv_state bf16 [6144,3], recurrent_state fp32
# [16,128,128], 22 stage buffers, g fp32 [16]). The golden test runs all
# THREE state-transition scenarios — A first token (zero state), B
# consecutive (runtime chains p=0 -> p=1 from its own state), C seeded
# non-zero state — so the in-place state updates and the seeded read path are
# both exercised under the sanitizer.
#
#   $ compute-sanitizer --tool memcheck --error-exitcode 99 \
#       build/tests/test_qwen35_deltanet_golden \
#       build/data/qwen35_08b_l0.cudalm /root/models/Qwen3.5-0.8B-Base \
#       build/data/qwen35_golden_l0_A.cudalm \
#       build/data/qwen35_golden_l0_B.cudalm \
#       build/data/qwen35_golden_l0_C.cudalm \
#       /root/py311/venv/bin/python /root/code/cudalm-agent --no-gen
#   [scenario A: first token (zero state)] (layer=0 pos=0)
#   [scenario B: consecutive (p0->p1 chain)] (layer=0 pos=1)
#   [scenario C: seeded non-zero state] (layer=0 pos=0)
#   [PASS] test_qwen35_deltanet_golden
#   ========= ERROR SUMMARY: 0 errors
#
# Notes:
# - The --no-gen flag runs the CUDA-only path: it assumes the .cudalm + the
#   3 goldens already exist and skips the test's system()-spawned Python
#   convert / oracle steps. This avoids the compute-sanitizer / fork-exec
#   launcher hang documented in benchmarks/sanitizer_qwen35_full_attention.txt
#   (the CUDA 11.8 sanitizer reaper deadlocks after system()-spawned children).
#   The Python steps are CPU-only (no CUDA) and are byte-identical to the
#   non-sanitized run, so skipping them does not change the GPU memory surface
#   being checked.
# - recurrent_state stays FP32 (never bf16/quantized mixed); the sanitizer
#   covers its in-place update across the 2-step scenario B and the seeded
#   read/update in scenario C.
