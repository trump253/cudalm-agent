# compute-sanitizer memcheck — CUDALM v0.3 Phase A (Qwen3.5 full model)
#
# Host: RTX 2080 Ti (sm_75), CUDA 11.8, single device (CUDA_VISIBLE_DEVICES=0)
# Tool: /usr/local/cuda-11.8/bin/compute-sanitizer --tool memcheck
#
# Scope: the NEW whole-model CUDA allocation lifecycle introduced by
# Qwen35Model (v0.3 Phase A) — the COMPLETE 24-layer Qwen3.5-0.8B text model
# loaded from the real checkpoint in one shot:
#   * model embedding   : bf16 [248320, 1024] device buffer (H2D, ~509 MB);
#   * final RMSNorm     : bf16 [1024] device buffer;
#   * LM head           : TIED to the embedding (aliased, no separate alloc);
#   * 24 decoder layers : 18 x Qwen35DeltaNetLayer (conv_state bf16 [6144,3]
#                         + recurrent_state fp32 [16,128,128] + 22 stage bufs)
#                         + 6 x Qwen35FullAttentionLayer (KV cache bf16
#                         [n_kv, max_seq, head_dim] K+V, ~256 MB each) — all
#                         weight sets + stage buffers uploaded H2D (~250 MB);
# The test exercises the full lifecycle under the sanitizer:
#   * load()            : validate_full_model + upload every model-level tensor
#                         + construct all 24 per-layer runtimes (device allocs);
#   * seed every layer  : DeltaNet seed_state (H2D) + full-attention KV head
#                         memset (non-zero sentinel) to prove reset is a no-op
#                         detector;
#   * reset_state()     : zero EVERY layer's persistent state (DeltaNet
#                         conv+recurrent, full-attention whole KV) across all 24;
#   * destroy (unload)  : every device buffer freed in the correct order
#                         (layer runtimes before the weight sets they reference).
#
# Note: run with the test's --no-gen flag (CUDA-only path) so compute-sanitizer
# does not wrap a std::system Python child (CUDA 11.8 reaper deadlock); the
# full-model .cudalm is pre-generated by convert_qwen35.py --full-model.
#
#   $ /usr/local/cuda-11.8/bin/compute-sanitizer --tool memcheck \
#       build/tests/test_qwen35_full_model \
#       build/data/qwen35_08b_full.cudalm /root/models/Qwen3.5-0.8B-Base \
#       /root/py311/venv/bin/python <src_dir> --no-gen
#
# Result:
========= COMPUTE-SANITIZER
[PASS] qwen35_full_model (structure + ownership + reset-all-24)
========= ERROR SUMMARY: 0 errors
#
# => 0 memory errors across the full-model load + 24-layer seed + reset +
#    unload (the new CUDA allocation lifecycle is clean).
